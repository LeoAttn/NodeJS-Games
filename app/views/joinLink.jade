extends layout
//var room = require('../controllers/Rooms');
block content
    h1= title
    p Welcome to #{title}

    div.list-room
        h3 Rejoignez une partie !
        table.table
            thead
                th Nom de la partie
                th Créateur
                th IsPlaying
                th IsPrivate
                th Rejoindre
            tr
                td=room.name
                td=room.creator
                td=room.playing
                td=room.private
                td
                    form(action="/join" method="post")
                        button(name="id" value=room._id) Rejoindre
